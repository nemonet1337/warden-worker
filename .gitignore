# Rust
/target/

# Node / Wrangler
/node_modules/
/.wrangler/
/build/

# Frontend (downloaded at build/dev time; public/css/ stays tracked)
/public/web-vault/
/bw_web_*.tar.gz

# Optional local clones of upstream reference repos
/vaultwarden/
/bw_web_builds/

# Generated seed SQL (see scripts/generate-global-domains-seed.py)
/sql/global_domains_seed.sql

# Secrets / local credentials
.env
.env.*
!.env.example
.dev.vars

# OS junk
.DS_Store
Thumbs.db
Desktop.ini

# Agent / MCP local state
# Committed on purpose: .claude/settings.json, .claude/rules/, .grok/config.toml
.claude/settings.local.json
.grok/*
!.grok/config.toml
.mcp-auth/
.headroom/
.kilo/
.cursor/
