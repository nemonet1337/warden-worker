{
  "name": "warden-worker",
  "version": "0.7.2",
  "private": true,
  "description": "Minimal Bitwarden-compatible backend on Cloudflare Workers (Rust/WASM).",
  "scripts": {
    "build": "cargo install --locked -q worker-build --version 0.8.5 && worker-build --release --locked",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
